<div class="d-flex justify-content-center">
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  
<% if @user.errors.any? %>
  <%= @user.errors.count %>error prohibited this obj from being saved:
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


      
  <div class="d-flex justify-content-center bg:#505D6B" style="background-color: #505d6b">
    <h2 class="text-white">プロフィール編集</h2>
  </div>
    <%= form_with model: @user do |f| %>
      <div class="form-group">
        <p for="inputName">名前</p>
        <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"名前"%>
      </div>
      <div class="form-group">
        <p for="inputIntroduction">プロフィール</p>
        <%= f.text_field :introduction, autofocus: true, id:"inputintroduction"%>
      </div>
      <div class="form-group">
        <%= f.submit "編集完了", class: "btn btn-secondary" %>
      </div>
    </div>
 </div>
</div>
   </div>
  </div>
 </div>
</div>
<% end %>